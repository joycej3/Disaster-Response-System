
frontend:
  trigger:
    include:
       - local: frontend/.frontend_gitlab_ci.yml
    strategy: depend

backend:
  trigger:
    include:
       - local: backend/backend_gitlab_ci.yml
    strategy: depend

.deploy:
  stage: deploy
  script: echo "Define your deployment script!"
  environment: production
